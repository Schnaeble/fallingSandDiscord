// Frontend Configuration
// This file should be created but NOT committed with real values
// Copy config.example.js to config.js and fill in your values

const CONFIG = {
    // Your Discord Application Client ID
    DISCORD_CLIENT_ID: 'YOUR_CLIENT_ID_HERE',
    
    // Your backend URL (where the OAuth server is hosted)
    BACKEND_URL: 'https://your-backend.railway.app'
};

// For GitHub Pages deployment, you can also use this approach:
// Set these in your repository secrets and use GitHub Actions to inject them
